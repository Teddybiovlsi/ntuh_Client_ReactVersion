# 複製本檔為 .env（放在專案根目錄，與 package.json 同層）後填入實際值。
# .env 已列在 .gitignore 中，不會被提交。
#
# 注意：Vite 只會處理 VITE_ 前綴的變數。
# 舊版的 REACT_APP_ 前綴（Create React App 時代）在 Vite 下完全無效。

# 後端 API 位址（結尾需保留斜線）
VITE_API_BASE_URL=https://ilchp01.yuntech.edu.tw/ntuh_api/public/index.php/

# 後端 Basic 驗證憑證，格式為 帳號:密碼（程式會自動做 base64 編碼）
# 留空則不會送出 Authorization header。
#
# ⚠️ 重要：Vite 在 build 時會把這個值直接內嵌進輸出的 JavaScript，
# 任何人在瀏覽器 devtools 中都能看到。這裡使用環境變數是為了讓設定
# 與程式碼分離、方便日後更換，而不是把它藏起來。
# 若需要真正的機密性，必須改由後端代理，不能放在前端。
VITE_API_BASIC_AUTH=
